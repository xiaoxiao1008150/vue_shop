// import * as types from './mutation-types'

const mutations = {
  setUser (state, user) {
    state.user = user
  }
  // [types.ADD_TO_CART] (state, cart) {
  //   state.cart = cart
  // }
}

export default mutations
